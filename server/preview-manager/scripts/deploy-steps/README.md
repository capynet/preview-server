# Deploy Steps

> **DEPRECATED**: These scripts are no longer executed by the current deployment flow.
> All deploy execution is handled by the VM Agent running on each preview VM.
> Project-specific deploy scripts are configured in `preview.yml` and executed
> by the agent directly inside the PHP container on the VM.
