# DEPRECATED: /var/www/previews is no longer used.
# Previews run on individual VMs managed by the VM Agent.
# This playbook is kept for reference only.
---
- name: "[DEPRECATED] Fix /var/www/previews permissions"
  hosts: preview-server
  become: yes
  tasks:
    - name: This playbook is deprecated
      debug:
        msg: "/var/www/previews is no longer used — previews run on individual VMs"
