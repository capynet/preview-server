---
# Setup Cloudflare R2 bucket
#
# Creates the bucket and configures lifecycle rules.
#
# Usage:
#   ansible-playbook playbooks/setup-r2.yml

- name: Setup Cloudflare R2
  hosts: druploy-server
  become: yes

  roles:
    - r2
